public abstract class LocationServer extends NanoHTTPD {
  public Response serve(String uri, String method, Properties header, Properties parms, Properties Files) {
     Location location = MainActivity.getLocation();
     //json. jsonObject = new 
     //jsonObject.addString("Error","Location Not Initialized");
  }
  
  //private Response Testing();
  
}
